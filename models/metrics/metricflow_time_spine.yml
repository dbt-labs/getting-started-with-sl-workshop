models:
  - name: metricflow_time_spine
    description: "MetricFlow time spine model used for MetricFlow time joins"
    time_spine:
      standard_granularity_column: date_day         # column used for the "standard" granularity
    columns:
      - name: date_day
        description: "Base date column for daily granularity"
        granularity: day