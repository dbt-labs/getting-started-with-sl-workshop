version: 2

models:
  - name: metricflow_time_spine
    description: Daily calendar dates for time-based reporting and metric analysis.
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        description: Calendar date for the row.
        granularity: day
