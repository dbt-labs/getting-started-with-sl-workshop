version: 2

models:
  - name: time_spine_daily
    description: Daily time spine used by MetricFlow for time-based metric queries.
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        granularity: day
